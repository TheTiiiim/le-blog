<h1>Users</h1>
<ul>
    {{#each users as |user|}}
        <li>
            <div>
                <h2>{{user.name}}</h2>
                <p>{{user.email}}</p>
            </div>
        </li>
    {{/each}}
</ul>
