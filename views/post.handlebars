<div class="post">
    <div class="userInfo flexContainer">
        <div class="profilePicture"></div>
        <a href="/user/{{post.user.id}}"><span>{{post.user.name}}</span></a>
    </div>
    <span class="optionsIcon"></span>
    <div class="postContent">
        <h1>{{post.title}}</h1>
        <p>{{post.content}}</p>
        <p>user id: {{post.user_id}}</p>
    </div>
</div>
